import "@/app/globals.css";

export default function Jobs(){
    return (
        <div className="flex flex-col justify-center bg-white">
            <h1>Job pages, not yet implemented</h1>
            <p>Allow users to search for jobs just like the costcode finder</p>
        </div>
    )

}